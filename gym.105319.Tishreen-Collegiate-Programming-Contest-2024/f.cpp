#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
        if (s == "BdnaDars") {
            cout << "Enough!\n";
        } else {
            cout << "OK\n";
        }
    }
}
